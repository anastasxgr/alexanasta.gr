import React from 'react'

import Icon from './Icon'


function IconsContainer() {

    return (
        <div className="icons">
            <Icon x={0} y={0} ic="terminal" />
            <Icon x={0} y={0} ic="terminal" />
            <Icon x={0} y={0} ic="terminal" />
            <Icon x={0} y={0} ic="terminal" />
        </div>
    )
}

export default IconsContainer